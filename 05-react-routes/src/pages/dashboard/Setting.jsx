import React from "react";
import "../../app.css";

import { Outlet, NavLink } from "react-router-dom";

const Setting = () => {
  return (
    <div>
      <h2>Setting</h2>
    </div>
  );
};

export default Setting;
